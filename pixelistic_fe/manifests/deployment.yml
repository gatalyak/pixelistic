apiVersion: apps/v1
kind: Deployment
metadata:
  name: pixelisticfe 
  labels:
    app: pixelisticfe
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pixelisticfe
  template:
    metadata:
      labels:
        app: pixelisticfe 
    spec:
      containers:
        - name: pixelisticfe 
          image: __REP_WEB_URI__:latest
          ports:
          - containerPort: 80
          
